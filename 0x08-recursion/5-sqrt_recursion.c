#include "main.h"

/**
 * _sqrt_recursion - returns the natural square root of a number
 * @n: integer to be square rooted
 * Return: the natural square root found
 */

int _sqrt_recursion(int n)
{

	int a = 1;

	return (sqrt_recursion(a, n));
}
